orbs:
  ssh-deployment: arturgontijo/ssh-deployment@dev:0.0.1

workflows:
  deploy_application:
    jobs:
    - ssh-deployment/deploy:
        ssh-host: ${SSH_HOST}
        ssh-user: ${SSH_USER}
        docker-image: ""
        docker-container: ""
        docker-nvidia: false
        docker-volume: ""
        docker-vol-src: ""
        docker-vol-dst: ""
        extras: ""

version: 2.1
